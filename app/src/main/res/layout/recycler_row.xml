<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/instagram_background"
    android:padding="16dp"
    android:layout_margin="16dp">

    <!-- User Email -->
    <TextView
        android:id="@+id/textViewUserEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:text="user@example.com"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray" />

    <!-- Picture -->
    <ImageView
        android:id="@+id/imageViewPicture"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:scaleType="centerCrop"
        android:background="@drawable/instagram_image_background" />

    <!-- Caption -->
    <TextView
        android:id="@+id/textViewCaption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/instagram_caption_background"
        android:text="This is a caption for the picture."
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</LinearLayout>
